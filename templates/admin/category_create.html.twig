{% extends 'admin/base.html.twig' %}
{% block body %}
	<section class="container my-2  justify-content-center">
		<div class="row">
			<div class="col text-center m-4">
				<h1>Catégorie</h1>

				{{ form_start(form) }}
				{{ form_widget(form) }}
				<button type="submit" class="btn btn-primary btn-lg my-4" id="button_create_register">Enregistrer</button>
				{{ form_end(form) }}
			</div>
		</div>
	</section>
{% endblock %}
